# TODO

## Task: Traverse all cars using the car plate

- [ ] Update `index.html` to add traversal UI (input plate, start, previous/next, output area)
- [ ] Update `script.js` to implement traversal state + functions to navigate through all occupied cars starting from a given plate
- [ ] Sync traversal view after parking/removing (avoid stale car display)
- [ ] Quick manual test: park cars, start traversal on an existing plate, use next/previous through all cars, remove a car, verify traversal updates safely

